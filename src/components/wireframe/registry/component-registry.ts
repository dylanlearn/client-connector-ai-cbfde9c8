// This file is a placeholder for component-registry.ts
// We just need to fix the DeviceType import
import { DeviceType } from '@/components/wireframe/utils/responsive-utils';

export interface ComponentRegistryProps {
  deviceType?: DeviceType;
}

// This is just a placeholder to fix the import error
const ComponentRegistry = () => {
  return null;
};

export default ComponentRegistry;
