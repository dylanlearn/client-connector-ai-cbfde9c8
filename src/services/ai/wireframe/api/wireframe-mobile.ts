
export const generateMobileConsiderations = async (sections: any[]) => {
  // Simple implementation that returns mobile considerations
  return `
    - Navigation becomes hamburger menu on mobile
    - Stack features section vertically on mobile
    - Reduce padding on all sections
    - Use smaller font sizes
    - Optimize images for mobile loading
  `;
};
